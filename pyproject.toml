[tool.black]
line-length = 100
target-version = ["py311"]
exclude = ["frontend", "migrations"]

[tool.isort]
profile = "black"
line_length = 100
skip = ["frontend", "migrations"]

[tool.flake8]
max-line-length = 100
extend-ignore = ["E203","W503"]
exclude = ["frontend", "migrations", ".venv"]
